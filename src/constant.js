module.exports = {
    /* Disk block size (bytes) */
    BLOCK_SIZE: 4096,
    /* Number of replicas per block */
    REPLICATION_FACTOR: 3,
    /* Time to terminate a worker if not seen in this time (ms) */
    TIMEOUT: 100000,
}